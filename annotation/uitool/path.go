// Package uitool identifies the trusted Spice Agent TUI annotation tool.
package uitool

// Path is the fully qualified Go tool package authorized by a consuming
// application's root go.mod. Go owns its version, checksum, vendor, and replace
// behavior; Spice does not maintain a parallel plugin dependency system.
const Path = "github.com/spice-framework/spice-agent-tui/cmd/spice-agent-tui-annotations"
